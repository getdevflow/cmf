<?php

declare(strict_types=1);

use function App\Shared\Helpers\set_url_scheme;
use function Codefy\Framework\Helpers\config;
use function Qubus\Security\Helpers\esc_html;
use function Qubus\Support\Helpers\concat_ws;

$siteName = $title ?? 'This site';
$homeUrl = $homeUrl ?? set_url_scheme(concat_ws('//', config()->string('cms.main_site_url'), ''));
$supportUrl = $supportUrl ?? null;
$logoUrl = $logoUrl ?? null;
?>

<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title><?= esc_html($siteName); ?> is Archived</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex,nofollow">
    <style>
        :root {
            --bg: #0f172a;
            --card: rgba(255, 255, 255, .08);
            --card-border: rgba(255, 255, 255, .16);
            --text: #f8fafc;
            --muted: #cbd5e1;
            --accent: #38bdf8;
            --accent-dark: #0284c7;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            color: var(--text);
            background:
                    radial-gradient(circle at top left, rgba(56, 189, 248, .35), transparent 35%),
                    radial-gradient(circle at bottom right, rgba(99, 102, 241, .32), transparent 35%),
                    var(--bg);
            display: grid;
            place-items: center;
            padding: 2rem;
        }

        .archive-page {
            width: min(100%, 760px);
            text-align: center;
        }

        .archive-card {
            background: var(--card);
            border: 1px solid var(--card-border);
            border-radius: 28px;
            padding: clamp(2rem, 5vw, 4rem);
            backdrop-filter: blur(18px);
            box-shadow: 0 30px 80px rgba(0, 0, 0, .35);
        }

        .archive-logo {
            max-width: 120px;
            max-height: 80px;
            object-fit: contain;
            margin-bottom: 1.5rem;
        }

        .archive-badge {
            display: inline-flex;
            align-items: center;
            gap: .5rem;
            padding: .45rem .85rem;
            border-radius: 999px;
            background: rgba(56, 189, 248, .14);
            color: #bae6fd;
            font-size: .875rem;
            font-weight: 700;
            letter-spacing: .04em;
            text-transform: uppercase;
            margin-bottom: 1.25rem;
        }

        .archive-title {
            margin: 0;
            font-size: clamp(2rem, 6vw, 4rem);
            line-height: 1.05;
            letter-spacing: -0.045em;
        }

        .archive-message {
            max-width: 620px;
            margin: 1.25rem auto 0;
            color: var(--muted);
            font-size: clamp(1.05rem, 2vw, 1.25rem);
            line-height: 1.7;
        }

        .archive-actions {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: .85rem;
            margin-top: 2rem;
        }

        .archive-button {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 46px;
            padding: .8rem 1.15rem;
            border-radius: 12px;
            font-weight: 700;
            text-decoration: none;
            transition: transform .18s ease, background .18s ease;
        }

        .archive-button:hover {
            transform: translateY(-1px);
        }

        .archive-button-primary {
            color: white;
            background: var(--accent-dark);
        }

        .archive-button-primary:hover {
            background: var(--accent);
        }

        .archive-button-secondary {
            color: var(--text);
            background: rgba(255, 255, 255, .10);
            border: 1px solid rgba(255, 255, 255, .18);
        }

        .archive-footer {
            margin-top: 1.25rem;
            color: rgba(203, 213, 225, .75);
            font-size: .9rem;
        }
    </style>
</head>
<body>
<main class="archive-page" role="main">
    <section class="archive-card" aria-labelledby="archive-title">
        <?php if ($logoUrl): ?>
            <img
                src="<?= esc_html($logoUrl); ?>"
                alt="<?= esc_html($siteName); ?>"
                class="archive-logo"
            >
        <?php endif; ?>

        <div class="archive-badge">
            <span aria-hidden="true">●</span>
            Archived Site
        </div>

        <h1 id="archive-title" class="archive-title">
            <?= esc_html($siteName); ?> is no longer active.
        </h1>

        <p class="archive-message">
            This site has been archived and is no longer available to the public.
            Its contents may have been moved, removed, or preserved for historical reference.
        </p>

        <div class="archive-actions">
            <a href="<?= esc_html($homeUrl); ?>" class="archive-button archive-button-primary">
                Return to Main Site
            </a>

            <?php if ($supportUrl): ?>
                <a href="<?= esc_html($supportUrl); ?>" class="archive-button archive-button-secondary">
                    Contact Support
                </a>
            <?php endif; ?>
        </div>
    </section>

    <footer class="archive-footer">
        Error 410 — Site archived
    </footer>
</main>
</body>
</html>
