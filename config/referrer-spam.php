<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | When set to `null` (default), it uses the Matomo referrer spam
    | blacklist. Alternatively, you can remove `null`, and replace it
    | with your defined array of referrer spammers to block.
    |--------------------------------------------------------------------------
    */
    'blacklist' => null,
];
